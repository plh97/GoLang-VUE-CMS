{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server API (Debug)",
            "type": "go",
            "request": "launch",
            "mode": "debug", // 确保是 debug，自动编译
            "program": "${workspaceFolder}/cmd/server/main.go", 
            "args": [
            ],
            // 确保工作目录是项目根目录，以便正确加载配置文件
            "cwd": "${workspaceFolder}"
        }
    ]
}